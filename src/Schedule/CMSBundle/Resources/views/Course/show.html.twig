{% extends 'ScheduleCMSBundle:default:index.html.twig' %}
{% block bodyId %}course{% endblock %}

{% block body -%}
  <div class="row">
    <div class="col-md-6">
      <div class="well">
        <div class="row">
          <div class="col-lg-12"><h1>{{ entity.title }}</h1></div>
          <div class="col-lg-12">Pavadinimas: {{ entity.title }}</div>
          <div class="col-lg-12">
            <a href="{{ path('course_edit', { 'id': entity.id }) }}" class="btn btn-primary" role="button">Redaguoti</a>
            <a href="{{ path('course_new') }}" class="btn btn-success" role="button">Pridėti</a>
          </div>
        </div>
      </div>
    </div>
  </div>

{% endblock %}
